import {DB} from '../db.js';import {money,num,dateFa,esc,toast} from '../utils.js';
export async function renderAccounting(App){
 const rows=await DB.all('transactions');const income=rows.filter(x=>x.type==='income').reduce((s,x)=>s+x.amount,0),expense=rows.filter(x=>x.type==='expense').reduce((s,x)=>s+x.amount,0);
 App.setView(`<h1 class="page-title">حسابداری</h1><div class="stats-grid"><div class="card stat"><div class="stat-label">درآمد</div><div class="stat-value green">${money(income)}</div></div><div class="card stat"><div class="stat-label">هزینه</div><div class="stat-value red">${money(expense)}</div></div><div class="card stat"><div class="stat-label">خالص</div><div class="stat-value">${money(income-expense)}</div></div></div>
 <div class="section-title">ثبت تراکنش</div><div class="card card-pad"><form id="f" class="form-grid"><div class="field"><label>نوع</label><select name="type"><option value="income">درآمد</option><option value="expense">هزینه</option></select></div><div class="field"><label>مبلغ</label><input name="amount" type="number" required></div><div class="field full"><label>شرح</label><input name="description" required></div><div class="field"><label>حساب</label><input name="account" value="صندوق"></div><div class="field"><label>تاریخ</label><input name="date" type="date"></div><div class="field full"><button class="btn btn-primary">ثبت تراکنش</button></div></form></div>
 <div class="section-title">تراکنش‌های اخیر</div><div class="list">${rows.sort((a,b)=>b.createdAt-a.createdAt).slice(0,30).map(x=>`<div class="list-item"><div class="list-main"><div class="list-title">${esc(x.description)}</div><div class="list-sub">${esc(x.account||'')} · ${dateFa(x.createdAt)}</div></div><div class="list-value ${x.type==='income'?'green':'red'}">${x.type==='income'?'+':'-'} ${money(x.amount)}</div></div>`).join('')||'<div class="empty">تراکنشی ثبت نشده است.</div>'}</div>`);
 document.getElementById('f').onsubmit=async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.target));d.amount=Number(d.amount);d.createdAt=Date.now();await DB.add('transactions',d);toast('تراکنش ثبت شد');renderAccounting(App)}
}
